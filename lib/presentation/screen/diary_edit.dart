import 'package:flutter/material.dart';

class DiaryEdit extends StatelessWidget {
  const DiaryEdit({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text('DIARY EDIT'),
    );
  }
}
